<UserControl x:Class="Battleship.MVVM.View.ShipBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Battleship.MVVM.View"
             xmlns:viewModel="clr-namespace:Battleship.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="460"
             d:DesignWidth="840">

    <UserControl.DataContext>
        <viewModel:ShipBuilderViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0">
                <RadioButton Content="asd"
                             Foreground="White"
                             Command="{Binding PaintCommand}" />
                <RadioButton Content="None"
                             Foreground="White"
                             Command="{Binding StopPaintingCommand}" />
            </StackPanel>
            <StackPanel Grid.Row="1"
                        Width="180"
                        VerticalAlignment="Center">

                <Label Content="Enter the name of the ship set:"
                       Foreground="White" />

                <TextBox Text="{Binding ShipSetName, UpdateSourceTrigger=PropertyChanged}" />

            </StackPanel>

            <Button x:Name="SaveShip"
                    Content="Save ships"
                    Grid.Row="2"
                    Command="{Binding SaveShipsCommand}" />
        </Grid>

        <Viewbox Grid.Column="1"
                 Stretch="Uniform">
            <ContentControl Content="{Binding GridView}"
                            MinWidth="400"
                            MinHeight="400"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" />
        </Viewbox>
    </Grid>
</UserControl>
